import java.lang.*;
import java.util.*;
import java.io.*;/*  Problem description:  The count-and-say sequence is the sequence of integers with the first five terms as following:  1.     1  2.     11  3.     21  4.     1211  5.     111221  1 is read off as "one 1" or 11.  11 is read off as "two 1s" or 21.  21 is read off as "one 2, then one 1" or 1211.  Given an integer n, generate the nth term of the count-and-say sequence.  Note: Each term of the sequence of integers will be represented as a string.*/public class Count_And_Say {  public static void main(String[] args) {    for (int n = 1; n <= 20; n++) {      System.out.printf("%3d: %-1s\n", n, count_and_say_1(n));    }    System.out.println();  }  private static String count_and_say_1(int n) {    if (n <= 0) {      throw new IllegalArgumentException("Input n must > 0.");    }    String result = "1";    for (int i = 2; i <= n; i++) {      StringBuilder temp = new StringBuilder();      int count = 1;      char current_char = result.charAt(0);      for (int j = 1; j < result.length(); j++) {        if (result.charAt(j) == result.charAt(j - 1)) {          count++;        } else {          temp.append(count).append(current_char);          current_char = result.charAt(j);          count = 1;        }      }      temp.append(count).append(current_char);      result = temp.toString();    }    return result;  }}